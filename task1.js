function myMultiplication(a, b, c, d) {
  console.log(`Numbers are: (${a}, ${b}, ${c}, ${d})`);
  return a * b * c * d;
}
const result = myMultiplication(5, 5, 5, 5);

console.log(`The multiplication of the four numbers are: ${result}`);
